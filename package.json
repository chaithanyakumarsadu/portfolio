{
  "name": "portfolio-contact-backend",
  "version": "1.0.0",
  "private": true,
  "description": "Vercel serverless API that sends the portfolio contact form to email via Resend.",
  "scripts": {
    "dev": "vercel dev"
  },
  "devDependencies": {
    "vercel": "^37.0.0"
  }
}